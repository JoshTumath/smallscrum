<header>
  <h1 id="title">
    {{#link-to 'index'}}SmallScrum{{/link-to}}
  </h1>

  <nav>
    <ul>
      <li>
        {{#if session.isAuthenticated}}
          <button {{action 'logout'}}>Logout</button>
        {{else}}
          {{#link-to 'login'}}Login{{/link-to}}
        {{/if}}
      </li>
    </ul>
  </nav>
</header>

<main>
  {{outlet}}
</main>
